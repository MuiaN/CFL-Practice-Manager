/**
 * Re-exports the firm white-label config so components can
 * import it with a clean alias: import firmConfig from "@/lib/firmConfig"
 *
 * To white-label for a new firm, edit only:  shared/firmConfig.ts
 */
export { default } from "@shared/firmConfig";
