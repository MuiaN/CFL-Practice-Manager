# CFL Legal Practice Management System - Environment Variables

# Database Configuration
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://cfl_user:your_secure_password@localhost:5432/cfl_legal

# Session Secret (for JWT token signing)
# Generate with: openssl rand -base64 64
# IMPORTANT: Use a strong, random value in production
SESSION_SECRET=your_random_secret_key_minimum_32_characters_long_change_this

# Node Environment
# Options: development, production
NODE_ENV=development

# Server Port (optional)
# Default: 5000
# PORT=5000
